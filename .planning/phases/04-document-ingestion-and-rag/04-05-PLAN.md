---
phase: 04-document-ingestion-and-rag
plan: 05
type: execute
wave: 4
depends_on: ["04-03", "04-04"]
files_modified:
  - tests/test_rag_pipeline.py
  - tests/test_ingest_worker.py
  - tests/test_docproc.py
  - tests/test_tools_phase4.py
autonomous: true
requirements: [DOCP-01, DOCP-02, DOCP-03, DOCP-04, DOCP-05, DOCP-06, DOCP-07]

must_haves:
  truths:
    - "Unit tests validate RAG pipeline functions with mocked Qdrant and Ollama"
    - "Unit tests validate ARQ worker job function with mocked docproc and pipeline"
    - "Unit tests validate docproc endpoint with mocked Docling"
    - "Unit tests validate new tools (qdrant_search, job_status)"
    - "All tests pass without requiring running Docker services"
  artifacts:
    - path: "tests/test_rag_pipeline.py"
      provides: "Tests for init_embed_model, index_document, query_documents"
      min_lines: 40
    - path: "tests/test_ingest_worker.py"
      provides: "Tests for ARQ process_document job function"
      min_lines: 30
    - path: "tests/test_docproc.py"
      provides: "Tests for docproc /process and /health endpoints"
      min_lines: 30
    - path: "tests/test_tools_phase4.py"
      provides: "Tests for QdrantSearchTool and JobStatusTool"
      min_lines: 30
  key_links:
    - from: "tests/test_rag_pipeline.py"
      to: "src/core_api/rag/pipeline.py"
      via: "import and mock-based testing"
      pattern: "from rag.pipeline import"
    - from: "tests/test_ingest_worker.py"
      to: "src/core_api/workers/ingest_worker.py"
      via: "import and mock-based testing"
      pattern: "process_document"
---

<objective>
Create unit tests for all Phase 4 modules: RAG pipeline, ARQ worker, docproc service, and new tools. Tests use mocks for external services (Qdrant, Redis, Ollama, Docling) so they run without Docker.

Purpose: Validates the logic and wiring of Phase 4 code without requiring a running Docker stack. Catches integration bugs at the code level before full system testing.
Output: Four test files covering all Phase 4 modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-document-ingestion-and-rag/04-CONTEXT.md
@.planning/phases/04-document-ingestion-and-rag/04-02-SUMMARY.md
@.planning/phases/04-document-ingestion-and-rag/04-03-SUMMARY.md

@src/core_api/rag/pipeline.py
@src/core_api/rag/gpu_lock.py
@src/core_api/workers/ingest_worker.py
@src/core_api/tools/qdrant_search_tool.py
@src/core_api/tools/job_status_tool.py
@src/core_api/routers/ingest.py
@src/docproc/main.py

<interfaces>
<!-- Test targets from Plan 02: -->
```python
# rag/pipeline.py
def init_embed_model() -> None: ...
def index_document(client_id: str, pages: list[dict], file_name: str) -> int: ...
def query_documents(client_id: str, question: str, top_k: int = 5) -> list[dict]: ...

# rag/gpu_lock.py
async def acquire_gpu_lock(redis_client, ttl_ms=120000, max_retries=30, retry_delay=1.0) -> bool: ...
async def release_gpu_lock(redis_client) -> None: ...

# workers/ingest_worker.py
async def process_document(ctx: dict, file_path: str, client_id: str, file_name: str) -> dict: ...
```

<!-- Test targets from Plan 03: -->
```python
# tools/qdrant_search_tool.py
class QdrantSearchTool(BaseTool):
    name: str = "qdrant_search"
    def _run(self, query: str, top_k: int = 5) -> str: ...

# tools/job_status_tool.py  
class JobStatusTool(BaseTool):
    name: str = "job_status"
    def _run(self, job_id: str) -> str: ...

# routers/ingest.py
POST /ingest — IngestRequest -> IngestResponse
```

<!-- Test targets from Plan 01: -->
```python
# docproc/main.py
POST /process — ProcessRequest -> ProcessResponse
GET /health -> {"status": "ok"}
```

<!-- Existing test patterns from Phase 3 (conftest with crewai stubs): -->
Tests in tests/ directory use sys.path manipulation and conftest.py stubs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create unit tests for RAG pipeline and GPU lock</name>
  <files>tests/test_rag_pipeline.py</files>
  <read_first>
    - src/core_api/rag/pipeline.py (functions to test)
    - src/core_api/rag/gpu_lock.py (functions to test)
    - tests/ (existing test files for patterns — check conftest.py)
  </read_first>
  <behavior>
    - init_embed_model sets Settings.embed_model to OllamaEmbedding instance
    - _collection_name returns "maai_{client_id}_documents" format
    - index_document creates Documents with correct metadata (file_name, page_label, client_id)
    - index_document calls SemanticSplitterNodeParser and VectorStoreIndex
    - query_documents returns list of dicts with text, score, file_name, page_label keys
    - acquire_gpu_lock returns True when lock is available (redis SET returns True)
    - acquire_gpu_lock retries and returns False when lock is held (redis SET returns None)
    - release_gpu_lock calls redis.delete with correct key
  </behavior>
  <action>
Create tests/test_rag_pipeline.py with pytest tests. Use `unittest.mock.patch` to mock external dependencies (qdrant_client, llama_index modules, redis).

Add `sys.path.insert(0, ...)` for src/core_api at the top, following existing test patterns.

Tests for pipeline.py:
1. `test_init_embed_model` — mock OllamaEmbedding, verify Settings.embed_model is set
2. `test_collection_name` — call `_collection_name("testclient")`, assert returns `"maai_testclient_documents"`
3. `test_index_document` — mock QdrantClient, QdrantVectorStore, SemanticSplitterNodeParser, VectorStoreIndex. Call `index_document("client1", [{"page_no": 1, "text": "hello"}], "test.pdf")`. Verify splitter and index were called.
4. `test_query_documents` — mock VectorStoreIndex.from_vector_store, mock retriever.retrieve to return mock nodes with metadata. Verify returned dicts have correct keys.

Tests for gpu_lock.py:
5. `test_acquire_gpu_lock_success` — mock redis.set to return True. Assert acquire returns True.
6. `test_acquire_gpu_lock_timeout` — mock redis.set to return None. Set max_retries=2, retry_delay=0.01. Assert acquire returns False.
7. `test_release_gpu_lock` — mock redis.delete. Assert called with "maai:gpu_lock".

Use `@pytest.mark.asyncio` for async tests (gpu_lock). Add `pytest-asyncio` note in comments if not already a dependency.

Mock strategy: patch at the module level where the import happens (e.g., `rag.pipeline.QdrantClient`, not `qdrant_client.QdrantClient`).
  </action>
  <verify>
    <automated>cd "D:/MAAI Agent Platform" && python -c "import ast; ast.parse(open('tests/test_rag_pipeline.py').read()); print('test syntax ok')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_rag_pipeline.py contains `test_init_embed_model`
    - tests/test_rag_pipeline.py contains `test_collection_name`
    - tests/test_rag_pipeline.py contains `test_index_document`
    - tests/test_rag_pipeline.py contains `test_query_documents`
    - tests/test_rag_pipeline.py contains `test_acquire_gpu_lock_success`
    - tests/test_rag_pipeline.py contains `test_acquire_gpu_lock_timeout`
    - tests/test_rag_pipeline.py contains `test_release_gpu_lock`
    - tests/test_rag_pipeline.py contains `maai:gpu_lock`
    - tests/test_rag_pipeline.py uses `unittest.mock.patch` or `pytest.mock`
  </acceptance_criteria>
  <done>Seven unit tests covering RAG pipeline functions and GPU lock with mocked external services</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create unit tests for ingest worker, docproc, and tools</name>
  <files>tests/test_ingest_worker.py, tests/test_docproc.py, tests/test_tools_phase4.py</files>
  <read_first>
    - src/core_api/workers/ingest_worker.py (worker to test)
    - src/docproc/main.py (docproc to test)
    - src/core_api/tools/qdrant_search_tool.py (tool to test)
    - src/core_api/tools/job_status_tool.py (tool to test)
    - src/core_api/routers/ingest.py (router to test)
    - tests/ (existing conftest.py for stubs and patterns)
  </read_first>
  <behavior>
    - process_document calls docproc via HTTP, then index_document, returns chunk count
    - process_document acquires and releases GPU lock
    - process_document returns error dict when docproc fails
    - docproc /health returns 200 with {"status": "ok"}
    - docproc /process returns 404 for missing file
    - docproc /process returns 400 for unsupported file type
    - QdrantSearchTool._run returns formatted results with citations
    - QdrantSearchTool._run returns "No relevant documents" when empty
    - JobStatusTool has name="job_status"
    - POST /ingest returns 404 for missing file
    - POST /ingest returns 400 for unsupported extension
    - POST /ingest returns job_id on success
  </behavior>
  <action>
**tests/test_ingest_worker.py** — Tests for ARQ worker:

Add sys.path for src/core_api. Mock httpx, pipeline, gpu_lock.

1. `test_process_document_success` — mock ctx with http_client and redis. Mock http_client.post to return docproc success response. Mock index_document to return 5. Mock GPU lock acquire/release. Assert returns `{"status": "complete", "chunks": 5, "file_name": "test.pdf"}`.
2. `test_process_document_docproc_failure` — mock http_client.post to return error. Assert returns error dict.
3. `test_process_document_releases_lock_on_error` — mock http_client.post to raise exception. Assert release_gpu_lock is called (finally block).

Use `@pytest.mark.asyncio`.

**tests/test_docproc.py** — Tests for docproc FastAPI app:

Add sys.path for src/docproc. Use `fastapi.testclient.TestClient`.

1. `test_health` — GET /health, assert 200 and `{"status": "ok"}`.
2. `test_process_missing_file` — POST /process with nonexistent path, assert 404.
3. `test_process_unsupported_type` — POST /process with `.docx` file, assert 400.
4. `test_process_success` — mock DocumentConverter.convert to return a mock document with iterate_items returning mock elements with text and prov. Assert 200 with pages in response.

For test 4, use `unittest.mock.patch` on `main.DocumentConverter` (or however it's imported in docproc main.py). The mock needs to return a result.document with iterate_items().

**tests/test_tools_phase4.py** — Tests for new tools:

Add sys.path for src/core_api. Stub crewai if needed (follow conftest pattern).

1. `test_qdrant_search_tool_name` — assert QdrantSearchTool.name == "qdrant_search" (class-level access per Phase 3 pattern).
2. `test_qdrant_search_tool_returns_citations` — mock `query_documents` to return sample results. Call `_run(query="test")`. Assert output contains "Source:" (D-06 citation format).
3. `test_qdrant_search_tool_empty_results` — mock `query_documents` to return empty list. Assert output contains "No relevant documents".
4. `test_job_status_tool_name` — assert JobStatusTool.name == "job_status".

Mock strategy: patch `tools.qdrant_search_tool.query_documents` and `tools.job_status_tool.create_pool`.
  </action>
  <verify>
    <automated>cd "D:/MAAI Agent Platform" && python -c "import ast; ast.parse(open('tests/test_ingest_worker.py').read()); ast.parse(open('tests/test_docproc.py').read()); ast.parse(open('tests/test_tools_phase4.py').read()); print('all test files syntax ok')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_ingest_worker.py contains `test_process_document_success`
    - tests/test_ingest_worker.py contains `test_process_document_docproc_failure`
    - tests/test_ingest_worker.py contains `acquire_gpu_lock` (mock reference)
    - tests/test_docproc.py contains `test_health`
    - tests/test_docproc.py contains `test_process_missing_file`
    - tests/test_docproc.py contains `test_process_success`
    - tests/test_tools_phase4.py contains `test_qdrant_search_tool_name`
    - tests/test_tools_phase4.py contains `test_qdrant_search_tool_returns_citations`
    - tests/test_tools_phase4.py contains `Source:` (verifying citation format)
    - tests/test_tools_phase4.py contains `test_job_status_tool_name`
  </acceptance_criteria>
  <done>Unit tests cover ingest worker (success, failure, lock release), docproc endpoints (health, missing file, success), and Phase 4 tools (name verification, citation format, empty results)</done>
</task>

</tasks>

<verification>
- All test files have valid Python syntax
- Tests use mocks for all external services (no real Docker services needed)
- Tests cover success paths, error paths, and edge cases
- Citation format (Source: filename, page N) is verified in tool tests
- GPU lock acquire/release is verified in worker tests
</verification>

<success_criteria>
- 4 test files exist with comprehensive unit tests
- All tests pass syntax validation
- RAG pipeline tests cover init, index, query, GPU lock
- Worker tests cover success, failure, lock cleanup
- Docproc tests cover health, validation, processing
- Tool tests verify names, citation format, empty results
</success_criteria>

<output>
After completion, create `.planning/phases/04-document-ingestion-and-rag/04-05-SUMMARY.md`
</output>

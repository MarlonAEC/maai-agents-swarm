---
phase: 04-document-ingestion-and-rag
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/phase04/__init__.py
  - tests/phase04/conftest.py
  - tests/phase04/test_docling_pipeline.py
  - tests/phase04/test_ocr_processing.py
  - tests/phase04/test_qdrant_indexing.py
  - tests/phase04/test_client_isolation.py
  - tests/phase04/test_rag_query.py
  - tests/phase04/test_chat_upload.py
  - tests/phase04/test_gpu_scheduling.py
autonomous: true
requirements: [DOCP-01, DOCP-02, DOCP-03, DOCP-04, DOCP-05, DOCP-06, DOCP-07]

must_haves:
  truths:
    - "tests/phase04/ directory exists with conftest and stub test files"
    - "pytest tests/phase04/ -x -q runs and all stubs pass (marked xfail or skip)"
    - "Each DOCP requirement has at least one corresponding stub test file"
  artifacts:
    - path: "tests/phase04/__init__.py"
      provides: "Package init for phase04 tests"
    - path: "tests/phase04/conftest.py"
      provides: "Shared fixtures: mock Qdrant client, sample PDF paths, test client IDs, sys.path setup"
      min_lines: 30
    - path: "tests/phase04/test_docling_pipeline.py"
      provides: "Stub tests for DOCP-01 (Docling text extraction)"
      contains: "pytest.mark.xfail"
    - path: "tests/phase04/test_ocr_processing.py"
      provides: "Stub tests for DOCP-02 (OCR via EasyOCR backend in Docling)"
      contains: "pytest.mark.xfail"
    - path: "tests/phase04/test_qdrant_indexing.py"
      provides: "Stub tests for DOCP-03, DOCP-04 (chunking, Qdrant storage)"
      contains: "pytest.mark.xfail"
    - path: "tests/phase04/test_client_isolation.py"
      provides: "Stub tests for DOCP-04 (per-client collection isolation)"
      contains: "pytest.mark.xfail"
    - path: "tests/phase04/test_rag_query.py"
      provides: "Stub tests for DOCP-05 (RAG query skill)"
      contains: "pytest.mark.xfail"
    - path: "tests/phase04/test_chat_upload.py"
      provides: "Stub tests for DOCP-06 (chat upload + ingest trigger)"
      contains: "pytest.mark.xfail"
    - path: "tests/phase04/test_gpu_scheduling.py"
      provides: "Stub tests for DOCP-07 (GPU lock sequencing)"
      contains: "pytest.mark.xfail"
  key_links: []
---

<objective>
Create Wave 0 test stubs per VALIDATION.md: the tests/phase04/ directory with conftest.py and xfail-marked stub tests for every DOCP requirement. These stubs will be filled in by Plans 01-04 as implementation proceeds, and finalized in Plan 05.

Purpose: Satisfies Nyquist sampling rule by ensuring pytest can run from Wave 1 onward. Plans 01-04 will add pytest verify commands targeting these files.
Output: tests/phase04/ directory with 8 stub test files + conftest.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-document-ingestion-and-rag/04-CONTEXT.md
@.planning/phases/04-document-ingestion-and-rag/04-VALIDATION.md

@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/phase04/ directory with conftest and all stub test files</name>
  <files>tests/phase04/__init__.py, tests/phase04/conftest.py, tests/phase04/test_docling_pipeline.py, tests/phase04/test_ocr_processing.py, tests/phase04/test_qdrant_indexing.py, tests/phase04/test_client_isolation.py, tests/phase04/test_rag_query.py, tests/phase04/test_chat_upload.py, tests/phase04/test_gpu_scheduling.py</files>
  <action>
Create tests/phase04/ directory with these files:

**tests/phase04/__init__.py** — Empty file.

**tests/phase04/conftest.py** — Shared fixtures for Phase 4 tests:

```python
"""Phase 4 test fixtures — document ingestion and RAG."""
import sys
import os
from pathlib import Path
from unittest.mock import MagicMock

import pytest

# Add src/core_api and src/docproc to sys.path for imports
CORE_API_DIR = str(Path(__file__).resolve().parent.parent.parent / "src" / "core_api")
DOCPROC_DIR = str(Path(__file__).resolve().parent.parent.parent / "src" / "docproc")
if CORE_API_DIR not in sys.path:
    sys.path.insert(0, CORE_API_DIR)
if DOCPROC_DIR not in sys.path:
    sys.path.insert(0, DOCPROC_DIR)

# Stub heavy dependencies that may not be installed in test env
# (same pattern as tests/conftest.py from Phase 3)
for mod_name in [
    "docling", "docling.document_converter", "docling.datamodel.pipeline_options",
    "docling.datamodel.base_models", "easyocr",
    "llama_index", "llama_index.core", "llama_index.core.node_parser",
    "llama_index.embeddings.ollama", "llama_index.vector_stores.qdrant",
    "qdrant_client", "arq", "arq.connections", "arq.jobs",
    "redis", "redis.asyncio",
]:
    if mod_name not in sys.modules:
        sys.modules[mod_name] = MagicMock()


@pytest.fixture
def sample_pages():
    """Sample docproc response pages for testing."""
    return [
        {"page_no": 1, "text": "This is page one content.", "has_ocr": False},
        {"page_no": 2, "text": "This is page two with OCR text.", "has_ocr": True},
    ]


@pytest.fixture
def test_client_id():
    """Test client ID for per-client isolation tests."""
    return "test_client_01"


@pytest.fixture
def sample_file_name():
    """Sample file name for ingestion tests."""
    return "test_document.pdf"
```

**Stub test files** — Each file follows this pattern:
- Module docstring explaining what DOCP requirement it covers
- One or more test functions marked `@pytest.mark.xfail(reason="stub — implementation in Plan XX")`
- Each test has a descriptive name and `assert False` body as placeholder

Create these stubs:

1. **test_docling_pipeline.py** (DOCP-01): `test_text_pdf_extraction`, `test_process_endpoint_returns_pages`
2. **test_ocr_processing.py** (DOCP-02): `test_scanned_pdf_ocr_enabled`, `test_easyocr_options_configured`
3. **test_qdrant_indexing.py** (DOCP-03, DOCP-04): `test_semantic_chunking`, `test_qdrant_vector_upsert`, `test_init_embed_model_768_dims`
4. **test_client_isolation.py** (DOCP-04): `test_collection_naming_per_client`, `test_query_returns_only_own_docs`
5. **test_rag_query.py** (DOCP-05): `test_rag_skill_returns_answer`, `test_qdrant_search_tool_citations`
6. **test_chat_upload.py** (DOCP-06): `test_ingest_endpoint_queues_job`, `test_ingest_skill_triggers_arq`
7. **test_gpu_scheduling.py** (DOCP-07): `test_gpu_lock_acquire_release`, `test_gpu_lock_serializes_workloads`
  </action>
  <verify>
    <automated>cd "D:/MAAI Agent Platform" && python -m pytest tests/phase04/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>tests/phase04/ exists with conftest.py and 7 stub test files. All stubs are xfail-marked and pytest runs without errors. Each DOCP requirement has at least one corresponding stub file.</done>
</task>

</tasks>

<verification>
- `pytest tests/phase04/ -x -q` runs and reports xfail results (no errors)
- Every DOCP-01 through DOCP-07 has at least one stub test
- conftest.py sets up sys.path for both src/core_api and src/docproc
- conftest.py stubs heavy dependencies (docling, llama_index, qdrant_client, arq, redis)
</verification>

<success_criteria>
- tests/phase04/ directory exists with __init__.py, conftest.py, and 7 test files
- pytest runs cleanly with all stubs marked xfail
- Stubs align with VALIDATION.md file naming convention
</success_criteria>

<output>
After completion, create `.planning/phases/04-document-ingestion-and-rag/04-00-SUMMARY.md`
</output>

---
phase: 01-infrastructure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - clients/default/client.env.example
  - clients/default/client.env
  - clients/default/models.yaml
  - config/litellm/proxy_config.yaml
  - .env
  - tests/__init__.py
  - tests/phase1/__init__.py
  - tests/phase1/conftest.py
  - tests/phase1/test_stack_startup.py
  - tests/phase1/test_local_only.py
  - tests/phase1/test_client_config.py
  - tests/phase1/test_gpu_active.py
  - tests/phase1/test_litellm_routing.py
  - tests/phase1/test_networking.py
  - pytest.ini
  - pyproject.toml
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-06

must_haves:
  truths:
    - "docker-compose.yml defines exactly three services (Ollama GPU, Ollama CPU, LiteLLM, Open WebUI) with correct images"
    - "All services connect via named network maai-net, no localhost references between containers"
    - "Client config folder clients/default/ contains env and models files with all required variables"
    - "LiteLLM proxy_config.yaml routes three model aliases to Ollama via container DNS"
    - "Test scaffold exists with stubs for all six INFRA requirements"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Complete Docker Compose stack definition"
      contains: "maai-net"
    - path: "clients/default/client.env.example"
      provides: "Template client config with documented defaults"
      contains: "WEBUI_PORT"
    - path: "clients/default/models.yaml"
      provides: "Config-driven model list for bootstrap"
      contains: "reasoning"
    - path: "config/litellm/proxy_config.yaml"
      provides: "LiteLLM model routing configuration"
      contains: "reasoning-model"
    - path: "pytest.ini"
      provides: "Test runner configuration"
      contains: "testpaths"
  key_links:
    - from: "docker-compose.yml"
      to: "clients/default/client.env"
      via: "env_file directive"
      pattern: "env_file.*client\\.env"
    - from: "docker-compose.yml"
      to: "config/litellm/proxy_config.yaml"
      via: "volume mount"
      pattern: "proxy_config\\.yaml:/app/config\\.yaml"
    - from: "config/litellm/proxy_config.yaml"
      to: "ollama service"
      via: "api_base URL"
      pattern: "http://ollama:11434"
---

<objective>
Create the Docker Compose stack definition, all client configuration files, LiteLLM proxy config, and the Phase 1 test scaffold. After this plan, the complete infrastructure-as-code exists and test stubs are ready for verification.

Purpose: Establishes the entire file structure for the Docker stack so bootstrap.sh (Plan 02) can operate on real config files.
Output: docker-compose.yml, client config folder, LiteLLM config, pytest scaffold with stubs for all 6 INFRA requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-CONTEXT.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md
@.planning/phases/01-infrastructure-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create test scaffold (Wave 0 — Nyquist compliance)</name>
  <files>
    tests/__init__.py,
    tests/phase1/__init__.py,
    tests/phase1/conftest.py,
    tests/phase1/test_stack_startup.py,
    tests/phase1/test_local_only.py,
    tests/phase1/test_client_config.py,
    tests/phase1/test_gpu_active.py,
    tests/phase1/test_litellm_routing.py,
    tests/phase1/test_networking.py,
    pytest.ini,
    pyproject.toml
  </files>
  <read_first>
    .planning/phases/01-infrastructure-foundation/01-VALIDATION.md
  </read_first>
  <action>
    1. Create `pyproject.toml` with minimal project metadata:
       - `[project]` section: name = "maai-agent-platform", version = "0.1.0", requires-python = ">=3.11"
       - `[project.optional-dependencies]` section: dev = ["pytest>=7.0", "pytest-asyncio>=0.23", "httpx>=0.27"]
       - `[tool.pytest.ini_options]` section: testpaths = ["tests"], asyncio_mode = "auto"

    2. Create `pytest.ini`:
       ```ini
       [pytest]
       testpaths = tests
       asyncio_mode = auto
       ```

    3. Create `tests/__init__.py` and `tests/phase1/__init__.py` as empty package markers.

    4. Create `tests/phase1/conftest.py` with shared fixtures:
       - `stack_base_url` fixture returning "http://localhost" (for tests that run against live stack)
       - `ollama_url` fixture returning "http://localhost:11434"
       - `litellm_url` fixture returning "http://localhost:4000"
       - `webui_port` fixture that reads WEBUI_PORT from clients/default/client.env, defaulting to 3000
       - `async_client` fixture returning an httpx.AsyncClient with timeout=30

    5. Create test stub files — each contains at minimum one test function with `pytest.skip("Requires running Docker stack")` for live tests, and one structural test that validates config files exist (these can run without Docker):

       `test_stack_startup.py` (INFRA-01):
       - `test_compose_file_exists()` — asserts docker-compose.yml exists at project root
       - `test_compose_file_has_required_services()` — loads YAML, asserts keys "ollama-gpu", "ollama-cpu", "litellm", "open-webui" exist in services
       - `test_stack_starts_all_services()` — skip("Requires running Docker stack")

       `test_local_only.py` (INFRA-02):
       - `test_no_cloud_api_keys_in_env()` — reads clients/default/client.env.example, asserts no lines contain "OPENAI_API_KEY=sk-" or "ANTHROPIC_API_KEY" or any cloud provider key
       - `test_litellm_config_points_to_ollama()` — reads proxy_config.yaml, asserts all api_base values contain "ollama:11434", none contain "api.openai.com" or external URLs
       - `test_no_external_endpoints()` — skip("Requires running Docker stack")

       `test_client_config.py` (INFRA-03):
       - `test_default_client_folder_exists()` — asserts clients/default/ directory exists
       - `test_client_env_has_required_vars()` — reads client.env.example, asserts contains WEBUI_PORT, OLLAMA_GPU_ENABLED, REASONING_MODEL, CLASSIFIER_MODEL, EMBEDDING_MODEL, LITELLM_MASTER_KEY, WEBUI_SECRET_KEY
       - `test_models_yaml_has_required_keys()` — reads models.yaml, asserts has keys: reasoning, classifier, embedding

       `test_gpu_active.py` (INFRA-04):
       - `test_compose_has_gpu_profile()` — loads docker-compose.yml, asserts ollama-gpu service has profiles: [gpu] and deploy.resources.reservations.devices with nvidia driver
       - `test_compose_has_cpu_profile()` — asserts ollama-cpu service has profiles: [cpu] and NO deploy.resources section
       - `test_gpu_detected_in_ollama_logs()` — skip("Requires running Docker stack with GPU")

       `test_litellm_routing.py` (INFRA-05):
       - `test_proxy_config_has_model_aliases()` — reads proxy_config.yaml, asserts model_name entries: "reasoning-model", "classifier-model", "embedding-model"
       - `test_proxy_config_uses_ollama_chat_prefix()` — asserts chat models use "ollama_chat/" prefix, embedding model uses "ollama/" prefix
       - `test_litellm_routes_to_correct_model()` — skip("Requires running Docker stack")

       `test_networking.py` (INFRA-06):
       - `test_compose_uses_named_network()` — loads docker-compose.yml, asserts top-level networks contains "maai-net"
       - `test_all_services_on_named_network()` — asserts every service in the compose file includes "maai-net" in its networks list
       - `test_no_localhost_in_service_config()` — reads docker-compose.yml as text, searches environment sections for "localhost" references to other services (should find none — only health checks use localhost which is correct)
       - `test_services_resolve_each_other()` — skip("Requires running Docker stack")

    6. Run `pip install pytest pyyaml httpx 2>/dev/null || uv pip install pytest pyyaml httpx` to ensure test deps available.
    7. Run `pytest tests/phase1/ --co -q` to verify all tests are collected without import errors.
  </action>
  <verify>
    <automated>pytest tests/phase1/ --co -q</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains `name = "maai-agent-platform"`
    - pytest.ini contains `testpaths = tests`
    - tests/__init__.py exists (can be empty)
    - tests/phase1/__init__.py exists (can be empty)
    - tests/phase1/conftest.py contains `def ollama_url` and `def litellm_url`
    - tests/phase1/test_stack_startup.py contains `def test_compose_file_exists`
    - tests/phase1/test_local_only.py contains `def test_no_cloud_api_keys_in_env`
    - tests/phase1/test_client_config.py contains `def test_client_env_has_required_vars`
    - tests/phase1/test_gpu_active.py contains `def test_compose_has_gpu_profile`
    - tests/phase1/test_litellm_routing.py contains `def test_proxy_config_has_model_aliases`
    - tests/phase1/test_networking.py contains `def test_compose_uses_named_network`
    - `pytest tests/phase1/ --co -q` exits 0 and collects >= 15 tests
  </acceptance_criteria>
  <done>All test stubs collected by pytest without errors. Structural tests ready to validate config files created in subsequent tasks.</done>
</task>

<task type="auto">
  <name>Task 1: Create Docker Compose stack definition</name>
  <files>docker-compose.yml, .env</files>
  <read_first>
    .planning/phases/01-infrastructure-foundation/01-RESEARCH.md,
    .planning/phases/01-infrastructure-foundation/01-CONTEXT.md
  </read_first>
  <action>
    Create `docker-compose.yml` at project root with the following exact structure (per D-01 through D-04, D-07, D-11):

    Top-level keys:
    - `name: maai-platform`
    - `networks:` with `maai-net:` using `driver: bridge`
    - `volumes:` with `ollama-models:` and `webui-data:`

    Services (4 service definitions, 3 logical services per D-01):

    **ollama-gpu:**
    - `container_name: ollama`
    - `image: ollama/ollama`
    - `profiles: [gpu]`
    - `volumes: [ollama-models:/root/.ollama]`
    - `deploy.resources.reservations.devices`: driver nvidia, count all, capabilities [gpu]
    - `ports: ["11434:11434"]`
    - `healthcheck.test`: `["CMD-SHELL", "wget -qO- http://localhost:11434/api/tags > /dev/null 2>&1 || exit 1"]`
    - `healthcheck.interval`: 30s, `timeout`: 10s, `retries`: 5, `start_period`: 60s
    - `networks: [maai-net]`
    - `restart: unless-stopped`

    **ollama-cpu:**
    - Identical to ollama-gpu EXCEPT: `profiles: [cpu]`, NO `deploy` section
    - Same container_name, image, volumes, ports, healthcheck, networks, restart

    **litellm:**
    - `container_name: litellm`
    - `image: ghcr.io/berriai/litellm:main-latest` with YAML comment: `# MUST resolve to >=1.83.0 — versions 1.82.7/1.82.8 were backdoored`
    - `command: ["--config", "/app/config.yaml"]`
    - `volumes: [./config/litellm/proxy_config.yaml:/app/config.yaml:ro]`
    - `env_file: [${CLIENT_ENV_PATH:-clients/default/client.env}]`
    - `ports: ["4000:4000"]`
    - `healthcheck.test`: `["CMD-SHELL", "wget -qO- http://localhost:4000/health/liveliness > /dev/null 2>&1 || exit 1"]`
    - `healthcheck.interval`: 15s, `timeout`: 5s, `retries`: 3, `start_period`: 40s
    - `depends_on`: both ollama-gpu and ollama-cpu with `condition: service_healthy` and `required: false`
    - `networks: [maai-net]`
    - `restart: unless-stopped`

    **open-webui:**
    - `container_name: open-webui`
    - `image: ghcr.io/open-webui/open-webui:main`
    - `environment:` (per D-03 — all traffic through LiteLLM):
      - `ENABLE_OLLAMA_API=false`
      - `ENABLE_OPENAI_API=true`
      - `OPENAI_API_BASE_URL=http://litellm:4000/v1`
      - `OPENAI_API_KEY=${LITELLM_MASTER_KEY}`
      - `WEBUI_SECRET_KEY=${WEBUI_SECRET_KEY}`
      - `WEBUI_AUTH=true`
      - `PORT=8080`
    - `env_file: [${CLIENT_ENV_PATH:-clients/default/client.env}]`
    - `ports: ["${WEBUI_PORT:-3000}:8080"]` (per D-04)
    - `volumes: [webui-data:/app/backend/data]`
    - `depends_on.litellm.condition: service_healthy`
    - `networks: [maai-net]`
    - `restart: unless-stopped`

    Create `.env` at project root (Compose automatically loads this):
    ```
    CLIENT_ENV_PATH=clients/default/client.env
    ```
    This allows overriding the client config path without editing docker-compose.yml.

    IMPORTANT: No `version:` key — Compose spec V2 uses `name:` instead. No localhost references between services (only in healthchecks where it refers to the container itself). No cloud API endpoints anywhere.
  </action>
  <verify>
    <automated>python -c "import yaml; d=yaml.safe_load(open('docker-compose.yml')); svcs=d['services']; assert 'ollama-gpu' in svcs; assert 'ollama-cpu' in svcs; assert 'litellm' in svcs; assert 'open-webui' in svcs; assert 'maai-net' in d['networks']; print('Compose structure valid')"</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml contains `name: maai-platform`
    - docker-compose.yml does NOT contain `version:`
    - docker-compose.yml contains `maai-net:` under networks
    - docker-compose.yml contains services: ollama-gpu, ollama-cpu, litellm, open-webui
    - ollama-gpu contains `profiles:` with `gpu` and `deploy:` with `nvidia`
    - ollama-cpu contains `profiles:` with `cpu` and does NOT contain `deploy:`
    - litellm contains `ghcr.io/berriai/litellm:main-latest`
    - litellm contains `proxy_config.yaml:/app/config.yaml`
    - litellm depends_on contains `required: false` for both ollama variants
    - open-webui contains `ENABLE_OLLAMA_API=false`
    - open-webui contains `OPENAI_API_BASE_URL=http://litellm:4000/v1`
    - open-webui contains `WEBUI_PORT:-3000`
    - All healthchecks use `wget` not `curl`
    - .env file contains `CLIENT_ENV_PATH=clients/default/client.env`
  </acceptance_criteria>
  <done>docker-compose.yml defines the complete Phase 1 stack with GPU/CPU profiles, health checks, named networking, and correct service chain. Structural tests from Task 0 pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create client config files and LiteLLM proxy config</name>
  <files>
    clients/default/client.env.example,
    clients/default/client.env,
    clients/default/models.yaml,
    config/litellm/proxy_config.yaml
  </files>
  <read_first>
    docker-compose.yml,
    .planning/phases/01-infrastructure-foundation/01-RESEARCH.md
  </read_first>
  <action>
    Per D-08, D-09, D-10, create the client config folder and LiteLLM config:

    1. Create `clients/default/client.env.example` with documented defaults:
       ```
       # MAAI Agent Platform — Client Configuration
       # Copy to client.env and fill in required values.
       # Generate secrets: openssl rand -hex 32

       # === Open WebUI ===
       WEBUI_PORT=3000
       WEBUI_SECRET_KEY=           # REQUIRED: generate with: openssl rand -hex 32

       # === LiteLLM ===
       LITELLM_MASTER_KEY=sk-maai-local   # Auth key for Open WebUI -> LiteLLM

       # === GPU Control ===
       # Auto-detected by bootstrap.sh. Override manually if needed.
       OLLAMA_GPU_ENABLED=true     # Set to "false" to force CPU profile

       # === Model Selection ===
       # bootstrap.sh reads these. CPU profile auto-downgrades to smaller models.
       REASONING_MODEL=qwen3:14b
       CLASSIFIER_MODEL=gemma3:4b
       EMBEDDING_MODEL=nomic-embed-text
       ```

    2. Create `clients/default/client.env` — copy of client.env.example but with a generated WEBUI_SECRET_KEY value. Use a deterministic placeholder that bootstrap.sh will replace: `WEBUI_SECRET_KEY=CHANGE_ME_RUN_BOOTSTRAP`. All other values same as example.

    3. Create `clients/default/models.yaml`:
       ```yaml
       # Model configuration for bootstrap.sh
       # GPU profile uses these directly; CPU profile auto-downgrades reasoning + classifier
       models:
         reasoning: qwen3:14b        # Overridden to qwen3:7b on CPU
         classifier: gemma3:4b       # Overridden to gemma3:1b on CPU
         embedding: nomic-embed-text  # Same on both profiles
       ```

    4. Create `config/litellm/proxy_config.yaml` (default GPU config — bootstrap.sh regenerates this):
       ```yaml
       # LiteLLM Proxy Configuration
       # Generated by bootstrap.sh — manual edits will be overwritten on next bootstrap run.
       # Model aliases: downstream consumers use these names, never raw Ollama model tags.
       model_list:
         - model_name: reasoning-model
           litellm_params:
             model: ollama_chat/qwen3:14b
             api_base: http://ollama:11434
             api_key: none
         - model_name: classifier-model
           litellm_params:
             model: ollama_chat/gemma3:4b
             api_base: http://ollama:11434
             api_key: none
         - model_name: embedding-model
           litellm_params:
             model: ollama/nomic-embed-text
             api_base: http://ollama:11434
             api_key: none
       general_settings:
         master_key: sk-maai-local
       litellm_settings:
         drop_params: true
       ```

    5. After creating all files, run the structural pytest tests to validate:
       `pytest tests/phase1/test_client_config.py tests/phase1/test_local_only.py tests/phase1/test_litellm_routing.py tests/phase1/test_networking.py tests/phase1/test_stack_startup.py tests/phase1/test_gpu_active.py -v -k "not skip and not running and not docker and not logs and not resolve and not external and not routes"` (run only structural tests, skip those requiring running Docker stack)
  </action>
  <verify>
    <automated>pytest tests/phase1/ -v -k "not Requires" --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - clients/default/client.env.example contains `WEBUI_PORT=3000`
    - clients/default/client.env.example contains `WEBUI_SECRET_KEY=`
    - clients/default/client.env.example contains `LITELLM_MASTER_KEY=sk-maai-local`
    - clients/default/client.env.example contains `OLLAMA_GPU_ENABLED=true`
    - clients/default/client.env.example contains `REASONING_MODEL=qwen3:14b`
    - clients/default/client.env.example contains `CLASSIFIER_MODEL=gemma3:4b`
    - clients/default/client.env.example contains `EMBEDDING_MODEL=nomic-embed-text`
    - clients/default/client.env exists and contains `WEBUI_PORT=3000`
    - clients/default/models.yaml contains `reasoning: qwen3:14b`
    - clients/default/models.yaml contains `embedding: nomic-embed-text`
    - config/litellm/proxy_config.yaml contains `model_name: reasoning-model`
    - config/litellm/proxy_config.yaml contains `ollama_chat/qwen3:14b`
    - config/litellm/proxy_config.yaml contains `ollama/nomic-embed-text`
    - config/litellm/proxy_config.yaml contains `http://ollama:11434`
    - config/litellm/proxy_config.yaml does NOT contain `api.openai.com` or any cloud URL
    - All non-skip structural tests pass (exit 0)
  </acceptance_criteria>
  <done>Client config folder and LiteLLM proxy config exist with correct values. All structural pytest tests pass, confirming files are properly formed and internally consistent.</done>
</task>

</tasks>

<verification>
After all three tasks complete:
1. `pytest tests/phase1/ --co -q` collects >= 15 tests without import errors
2. `pytest tests/phase1/ -v -k "not Requires"` — all structural tests pass (those not requiring Docker stack)
3. `docker-compose.yml` parses as valid YAML with correct service structure
4. No cloud API endpoints or secret keys in any committed file
5. All files referenced in docker-compose.yml exist (client.env, proxy_config.yaml)
</verification>

<success_criteria>
- docker-compose.yml defines 4 service definitions (ollama-gpu, ollama-cpu, litellm, open-webui) on maai-net
- clients/default/ contains client.env, client.env.example, models.yaml with all Phase 1 vars
- config/litellm/proxy_config.yaml routes 3 model aliases to Ollama
- Test scaffold covers all 6 INFRA requirements with structural + skip-marked live tests
- All structural tests pass without a running Docker stack
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-01-SUMMARY.md`
</output>

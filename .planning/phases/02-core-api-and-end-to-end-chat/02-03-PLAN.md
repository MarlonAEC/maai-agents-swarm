---
phase: 02-core-api-and-end-to-end-chat
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - docker-compose.yml
  - clients/default/client.env
  - clients/default/client.env.example
  - tests/phase2/__init__.py
  - tests/phase2/conftest.py
  - tests/phase2/test_core_api.py
  - tests/phase2/test_pipeline.py
  - tests/phase2/test_docker_wiring.py
autonomous: false
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - CHAT-04
  - CHAT-05
  - CHAT-06
  - AGNT-07
  - AGNT-08
  - AGNT-09

must_haves:
  truths:
    - "docker-compose.yml contains core-api and pipelines services on maai-net"
    - "Open WebUI OPENAI_API_BASE_URLS includes both LiteLLM and Pipelines endpoints"
    - "maai-uploads volume is shared between pipelines and core-api"
    - "core-api depends_on litellm service_healthy"
    - "Structural tests validate docker-compose.yml has new services, volumes, and env vars"
    - "User can send a chat message in Open WebUI and receive a response from the CrewAI agent"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Extended Docker Compose with core-api and pipelines services"
      contains: "core-api"
    - path: "clients/default/client.env"
      provides: "Updated client config with PIPELINES_API_KEY and CORE_API_PORT"
      contains: "PIPELINES_API_KEY"
    - path: "tests/phase2/test_docker_wiring.py"
      provides: "Structural tests for Docker Compose Phase 2 additions"
    - path: "tests/phase2/test_core_api.py"
      provides: "Core API endpoint tests"
    - path: "tests/phase2/test_pipeline.py"
      provides: "Pipeline plugin structural tests"
  key_links:
    - from: "docker-compose.yml open-webui"
      to: "pipelines service"
      via: "OPENAI_API_BASE_URLS semicolon-separated"
      pattern: "http://litellm:4000/v1;http://pipelines:9099"
    - from: "docker-compose.yml core-api"
      to: "litellm service"
      via: "depends_on service_healthy"
      pattern: "depends_on.*litellm.*service_healthy"
    - from: "docker-compose.yml pipelines"
      to: "src/pipelines volume mount"
      via: "volumes bind mount"
      pattern: "./src/pipelines:/app/pipelines"
---

<objective>
Wire the Core API and Pipelines services into Docker Compose, update client config, create the Phase 2 test scaffold, and verify the full end-to-end chat path.

Purpose: This plan connects all Phase 2 components into the running Docker stack and proves the pipeline works: Open WebUI -> Pipelines -> Core API -> CrewAI agent -> LiteLLM -> Ollama -> response back to user.

Output: Updated docker-compose.yml with 2 new services, updated client.env, test scaffold, and human-verified end-to-end chat.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-api-and-end-to-end-chat/02-CONTEXT.md
@.planning/phases/02-core-api-and-end-to-end-chat/02-RESEARCH.md
@docker-compose.yml
@clients/default/client.env
@clients/default/client.env.example
@.planning/phases/02-core-api-and-end-to-end-chat/02-01-SUMMARY.md
@.planning/phases/02-core-api-and-end-to-end-chat/02-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs: -->
src/core_api/Dockerfile: Python 3.11, uvicorn on port 8000, /health endpoint
src/core_api/main.py: FastAPI app, routes /health (GET) and /chat (POST)

<!-- From Plan 02 outputs: -->
src/pipelines/maai_pipe.py: Pipeline class, type=pipe, name="MAAI Agent"
  - Valves: CORE_API_URL="http://core-api:8000", REQUEST_TIMEOUT=120.0
  - Forwards to POST http://core-api:8000/chat

<!-- From Phase 1 (existing): -->
docker-compose.yml: ollama-gpu/cpu, litellm, open-webui on maai-net
  - open-webui env: OPENAI_API_BASE_URLS=http://litellm:4000/v1, OPENAI_API_KEYS=${LITELLM_MASTER_KEY}
  - volumes: ollama-models, webui-data
clients/default/client.env: WEBUI_PORT, WEBUI_SECRET_KEY, LITELLM_MASTER_KEY, OLLAMA_GPU_ENABLED, model vars

<!-- Research Pattern 4 — Pipelines connection via OPENAI_API_BASE_URLS: -->
OPENAI_API_BASE_URLS=http://litellm:4000/v1;http://pipelines:9099
OPENAI_API_KEYS=${LITELLM_MASTER_KEY};${PIPELINES_API_KEY}
Both must have same number of semicolon-separated entries.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Docker Compose with core-api and pipelines services</name>
  <files>
    docker-compose.yml
    clients/default/client.env
    clients/default/client.env.example
  </files>
  <read_first>
    docker-compose.yml
    clients/default/client.env
    clients/default/client.env.example
    .planning/phases/02-core-api-and-end-to-end-chat/02-RESEARCH.md
  </read_first>
  <action>
**Update docker-compose.yml** — Add two new services and one new volume to the existing file. Do NOT remove or modify existing services (ollama-gpu, ollama-cpu, litellm, open-webui) except the open-webui environment block.

Add to `volumes:` section:
```yaml
  maai-uploads:    # Shared between pipelines (write) and core-api (read) — per D-15
```

Add `core-api` service (per D-12, D-13):
```yaml
  # -- Core API (FastAPI + CrewAI) -----------------------------------------------
  core-api:
    container_name: core-api
    build:
      context: ./src/core_api
      dockerfile: Dockerfile
    env_file:
      - ${CLIENT_ENV_PATH:-clients/default/client.env}
    environment:
      - LITELLM_BASE_URL=http://litellm:4000/v1
      - LITELLM_MASTER_KEY=${LITELLM_MASTER_KEY:-sk-maai-local}
      - EMBEDDING_MODEL=${EMBEDDING_MODEL:-nomic-embed-text}
    volumes:
      - maai-uploads:/app/uploads
    ports:
      - "${CORE_API_PORT:-8000}:8000"
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8000/health')\""]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 30s
    depends_on:
      litellm:
        condition: service_healthy
    networks:
      - maai-net
    restart: unless-stopped
```

Add `pipelines` service (per D-01, D-12):
```yaml
  # -- Pipelines (Open WebUI plugin server) --------------------------------------
  pipelines:
    container_name: pipelines
    image: ghcr.io/open-webui/pipelines:main
    environment:
      - PIPELINES_API_KEY=${PIPELINES_API_KEY:-0p3n-w3bu!}
    volumes:
      - ./src/pipelines:/app/pipelines    # Mount plugin files directly — per D-13
      - maai-uploads:/app/uploads          # Shared upload volume — per D-15
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:9099/health')\""]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 20s
    networks:
      - maai-net
    restart: unless-stopped
```

**Update open-webui service environment** (per D-04 and research Pattern 4):
Change these two lines in the open-webui environment block:
```yaml
      - OPENAI_API_BASE_URLS=http://litellm:4000/v1;http://pipelines:9099
      - OPENAI_API_KEYS=${LITELLM_MASTER_KEY};${PIPELINES_API_KEY}
```
CRITICAL: Both env vars MUST have the same number of semicolon-separated entries — if OPENAI_API_BASE_URLS has 2 entries, OPENAI_API_KEYS must also have 2 entries. Mismatched counts cause silent failures.

Add open-webui depends_on for pipelines (in addition to existing litellm dependency):
```yaml
    depends_on:
      litellm:
        condition: service_healthy
      pipelines:
        condition: service_healthy
```

**Update clients/default/client.env** — Add new variables:
```
# === Core API ===
CORE_API_PORT=8000

# === Pipelines ===
PIPELINES_API_KEY=0p3n-w3bu!
```

**Update clients/default/client.env.example** — Add the same new variables with documentation:
```
# === Core API ===
CORE_API_PORT=8000              # Port for FastAPI Core API (default: 8000)

# === Pipelines ===
PIPELINES_API_KEY=0p3n-w3bu!   # API key for Pipelines server (must match OPENAI_API_KEYS second entry)
```
  </action>
  <verify>
    <automated>python -c "
import yaml
with open('docker-compose.yml') as f:
    dc = yaml.safe_load(f)
services = dc.get('services', {})
volumes = dc.get('volumes', {})

# Check new services exist
assert 'core-api' in services, 'Missing core-api service'
assert 'pipelines' in services, 'Missing pipelines service'

# Check volume
assert 'maai-uploads' in volumes, 'Missing maai-uploads volume'

# Check core-api config
ca = services['core-api']
assert 'maai-net' in ca.get('networks', []), 'core-api not on maai-net'
assert ca.get('healthcheck') is not None, 'core-api missing healthcheck'

# Check pipelines config
pp = services['pipelines']
assert 'maai-net' in pp.get('networks', []), 'pipelines not on maai-net'

# Check open-webui env has Pipelines URL
owui = services['open-webui']
env_list = owui.get('environment', [])
base_urls = [e for e in env_list if 'OPENAI_API_BASE_URLS' in str(e)]
assert any('pipelines:9099' in str(e) for e in base_urls), 'open-webui missing pipelines in OPENAI_API_BASE_URLS'

print('Docker Compose validation passed')
" && python -c "
with open('clients/default/client.env') as f:
    content = f.read()
assert 'PIPELINES_API_KEY' in content, 'Missing PIPELINES_API_KEY in client.env'
assert 'CORE_API_PORT' in content, 'Missing CORE_API_PORT in client.env'
print('client.env validation passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml contains `core-api` service with `build: context: ./src/core_api`
    - docker-compose.yml contains `pipelines` service with `image: ghcr.io/open-webui/pipelines:main`
    - docker-compose.yml contains `maai-uploads:` in volumes section
    - docker-compose.yml core-api has `depends_on: litellm: condition: service_healthy`
    - docker-compose.yml core-api has `maai-uploads:/app/uploads` volume mount
    - docker-compose.yml pipelines has `./src/pipelines:/app/pipelines` volume mount
    - docker-compose.yml pipelines has `maai-uploads:/app/uploads` volume mount
    - docker-compose.yml open-webui OPENAI_API_BASE_URLS contains `http://litellm:4000/v1;http://pipelines:9099`
    - docker-compose.yml open-webui OPENAI_API_KEYS contains `${LITELLM_MASTER_KEY};${PIPELINES_API_KEY}`
    - docker-compose.yml open-webui depends_on includes both litellm and pipelines
    - clients/default/client.env contains `CORE_API_PORT=8000`
    - clients/default/client.env contains `PIPELINES_API_KEY=0p3n-w3bu!`
    - clients/default/client.env.example contains `CORE_API_PORT` and `PIPELINES_API_KEY`
  </acceptance_criteria>
  <done>
    Docker Compose has 6 services (ollama-gpu/cpu, litellm, open-webui, core-api, pipelines) with maai-uploads shared volume. Open WebUI is wired to both LiteLLM and Pipelines. Client config has all new env vars.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 2 test scaffold</name>
  <files>
    tests/phase2/__init__.py
    tests/phase2/conftest.py
    tests/phase2/test_docker_wiring.py
    tests/phase2/test_core_api.py
    tests/phase2/test_pipeline.py
  </files>
  <read_first>
    docker-compose.yml
    tests/phase1/conftest.py
    tests/phase1/test_stack_startup.py
    src/core_api/routers/chat.py
    src/pipelines/maai_pipe.py
  </read_first>
  <action>
Create the Phase 2 test scaffold following the same conventions established in Phase 1 (structural tests that validate config without Docker, live tests that skip with clear messages).

**tests/phase2/__init__.py:** Empty file.

**tests/phase2/conftest.py:**
```python
"""Phase 2 test fixtures."""
import pytest
import yaml
import os

@pytest.fixture
def compose_config():
    """Load docker-compose.yml as parsed YAML."""
    with open("docker-compose.yml") as f:
        return yaml.safe_load(f)

@pytest.fixture
def client_env():
    """Load client.env as a dict."""
    env = {}
    with open("clients/default/client.env") as f:
        for line in f:
            line = line.strip()
            if line and not line.startswith("#") and "=" in line:
                key, _, value = line.partition("=")
                env[key.strip()] = value.strip()
    return env

@pytest.fixture
def core_api_url():
    """Core API base URL for live tests."""
    return os.getenv("CORE_API_URL", "http://localhost:8000")

@pytest.fixture
def pipelines_url():
    """Pipelines server base URL for live tests."""
    return os.getenv("PIPELINES_URL", "http://localhost:9099")
```

**tests/phase2/test_docker_wiring.py:** Structural tests for Docker Compose changes:
- `test_core_api_service_exists`: compose_config has 'core-api' in services
- `test_pipelines_service_exists`: compose_config has 'pipelines' in services
- `test_maai_uploads_volume_exists`: compose_config has 'maai-uploads' in volumes
- `test_core_api_on_maai_net`: core-api networks includes 'maai-net'
- `test_pipelines_on_maai_net`: pipelines networks includes 'maai-net'
- `test_core_api_depends_on_litellm`: core-api depends_on contains 'litellm'
- `test_core_api_healthcheck_defined`: core-api has healthcheck key
- `test_pipelines_healthcheck_defined`: pipelines has healthcheck key
- `test_core_api_uploads_volume_mount`: core-api volumes contains 'maai-uploads:/app/uploads'
- `test_pipelines_uploads_volume_mount`: pipelines volumes contains 'maai-uploads:/app/uploads'
- `test_pipelines_source_mount`: pipelines volumes contains './src/pipelines:/app/pipelines'
- `test_open_webui_base_urls_has_pipelines`: open-webui environment contains OPENAI_API_BASE_URLS with 'pipelines:9099'
- `test_open_webui_api_keys_count_matches_urls`: OPENAI_API_BASE_URLS and OPENAI_API_KEYS have the same number of semicolon entries (CRITICAL — mismatch causes silent failures per Pitfall 2)
- `test_client_env_has_pipelines_key`: client_env contains 'PIPELINES_API_KEY'
- `test_client_env_has_core_api_port`: client_env contains 'CORE_API_PORT'

**tests/phase2/test_core_api.py:** Core API structural + live tests:
- `test_core_api_main_exists`: os.path.exists('src/core_api/main.py')
- `test_core_api_dockerfile_exists`: os.path.exists('src/core_api/Dockerfile')
- `test_core_api_agents_yaml_exists`: os.path.exists('src/core_api/agents/config/agents.yaml')
- `test_core_api_tasks_yaml_exists`: os.path.exists('src/core_api/agents/config/tasks.yaml')
- `test_agents_yaml_has_freeform_agent`: Parse agents.yaml, assert 'freeform_agent' key exists
- `test_tasks_yaml_has_template_variables`: Parse tasks.yaml, assert '{messages}' and '{user_message}' in freeform_task.description
- `test_dockerfile_uses_python_311`: Read Dockerfile, assert 'python:3.11' in content
- `test_freeform_crew_uses_stream_false`: Read freeform_crew.py, assert 'stream=False' in content (AGNT-08)
- `test_freeform_crew_has_guardrails`: Read freeform_crew.py, assert 'max_iter=5' and 'max_execution_time=60' (AGNT-09)
- `test_freeform_crew_has_ollama_embedder`: Read freeform_crew.py, assert '"provider": "ollama"' in content (AGNT-07)
- `test_core_api_health_live` (skip if Docker not running): httpx GET {core_api_url}/health, assert 200 and {"status": "ok"}
- `test_core_api_chat_live` (skip if Docker not running): httpx POST {core_api_url}/chat with {"messages": [{"role": "user", "content": "hello"}], "user_message": "hello"}, assert 200 and response has "response" key

**tests/phase2/test_pipeline.py:** Pipeline plugin structural tests:
- `test_pipeline_file_exists`: os.path.exists('src/pipelines/maai_pipe.py')
- `test_pipeline_is_pipe_type`: Read maai_pipe.py, assert 'self.type = "pipe"' in content (NOT "filter")
- `test_pipeline_has_event_emitter`: Read maai_pipe.py, assert '__event_emitter__' in content
- `test_pipeline_targets_core_api`: Read maai_pipe.py, assert 'core-api:8000' in content
- `test_pipeline_has_upload_handling`: Read maai_pipe.py, assert '/app/uploads' in content
- `test_pipeline_has_valves`: Read maai_pipe.py, assert 'class Valves' in content
  </action>
  <verify>
    <automated>cd "D:\MAAI Agent Platform" && python -m pytest tests/phase2/ -v --co 2>/dev/null || python -m pytest tests/phase2/test_docker_wiring.py tests/phase2/test_core_api.py tests/phase2/test_pipeline.py -v -x 2>&1 | head -80</automated>
  </verify>
  <acceptance_criteria>
    - tests/phase2/__init__.py exists
    - tests/phase2/conftest.py contains `def compose_config` and `def client_env`
    - tests/phase2/test_docker_wiring.py contains at least 15 test functions
    - tests/phase2/test_docker_wiring.py contains `test_open_webui_api_keys_count_matches_urls`
    - tests/phase2/test_core_api.py contains `test_freeform_crew_uses_stream_false`
    - tests/phase2/test_core_api.py contains `test_freeform_crew_has_guardrails`
    - tests/phase2/test_core_api.py contains `test_freeform_crew_has_ollama_embedder`
    - tests/phase2/test_pipeline.py contains `test_pipeline_is_pipe_type`
    - All structural tests (non-live) pass: `pytest tests/phase2/ -v -k "not live"` exits 0
  </acceptance_criteria>
  <done>
    Phase 2 test scaffold with 25+ tests covering Docker wiring, Core API structure, pipeline plugin, and requirements AGNT-07/08/09. Structural tests pass without Docker running.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify end-to-end chat pipeline</name>
  <files>none</files>
  <action>
Human verification of the complete Phase 2 chat pipeline.

What was built: Complete Phase 2 chat pipeline: Open WebUI -> Pipelines pipe plugin -> Core API (FastAPI + CrewAI) -> LiteLLM -> Ollama -> response displayed in chat UI. New Docker services: core-api (FastAPI + CrewAI on port 8000) and pipelines (Open WebUI Pipelines on port 9099). Open WebUI now shows "MAAI Agent" as a selectable model.

Verification steps:

1. Rebuild and start the stack:
   ```
   docker compose build core-api
   docker compose --profile gpu up -d
   ```
   (Use `--profile cpu` if no GPU available)

2. Check all containers are healthy:
   ```
   docker compose --profile gpu ps
   ```
   Expected: ollama, litellm, open-webui, core-api, pipelines all "Up" and "healthy"

3. Verify Core API health:
   ```
   curl http://localhost:8000/health
   ```
   Expected: `{"status":"ok"}`

4. Open browser: http://localhost:3080 (or your WEBUI_PORT)
   - In the model selector dropdown, "MAAI Agent" should appear as a selectable option
   - Select "MAAI Agent"

5. Send a test message: "Hello, what can you help me with?"
   - Expected: You should see "Thinking..." status, then "Processing your request...", then a conversational response from the agent

6. Test multi-turn: Send a follow-up message referencing the previous response
   - Expected: Agent responds with awareness of the previous message context

7. Test file upload: Attach any small file (e.g., a .txt file) and send a message
   - Expected: Response includes "File received: {filename}. Document processing will be available in a future update."

8. Run structural tests:
   ```
   pytest tests/phase2/ -v -k "not live"
   ```
   Expected: All structural tests pass

9. (Optional) Run live tests:
   ```
   pytest tests/phase2/ -v
   ```
   Expected: Both structural and live tests pass

Resume signal: Type "approved" if the full chat pipeline works, or describe any issues encountered.
  </action>
  <verify>
    <automated>curl -s http://localhost:8000/health 2>/dev/null | python -c "import sys,json; d=json.load(sys.stdin); assert d['status']=='ok'; print('Core API healthy')" || echo "SKIP: Docker stack not running — manual verification required"</automated>
  </verify>
  <done>
    User has verified: (1) "MAAI Agent" appears in Open WebUI model dropdown, (2) sending a message returns a coherent response from CrewAI agent, (3) multi-turn context works, (4) file upload is acknowledged, (5) all structural tests pass.
  </done>
</task>

</tasks>

<verification>
- Docker Compose has 6 services (4 existing + 2 new) with correct networking and health checks
- Open WebUI connects to both LiteLLM and Pipelines via semicolon-separated OPENAI_API_BASE_URLS
- maai-uploads volume shared between pipelines and core-api
- All structural tests pass
- End-to-end chat verified by human
</verification>

<success_criteria>
- User sends a message in Open WebUI, selects "MAAI Agent", and receives a response from the CrewAI agent (CHAT-01, CHAT-04, CHAT-05)
- Chat history persists across browser sessions (CHAT-02 — Open WebUI handles this natively)
- File upload acknowledged (CHAT-03)
- Multi-turn context maintained (CHAT-06)
- All 9 Phase 2 requirements verified through structural tests and human verification
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-api-and-end-to-end-chat/02-03-SUMMARY.md`
</output>
